"use client";import{FormEvent,useState}from"react";import{useRouter}from"next/navigation";type S={id:number;name:string;url:string;is_active:boolean};export default function RssSources({initial}:{initial:S[]}){const r=useRouter();const[e,setE]=useState("");async function add(x:FormEvent<HTMLFormElement>){x.preventDefault();const f=new FormData(x.currentTarget);const res=await fetch("/api/rss/sources",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(f))});if(!res.ok){setE((await res.json()).error);return}x.currentTarget.reset();r.refresh()}return <><form onSubmit={add} className="card" style={{maxWidth:720}}><strong>Add RSS source</strong><div style={{display:"grid",gridTemplateColumns:"1fr 2fr auto",gap:10,marginTop:12}}><input name="name" required placeholder="Source name"/><input name="url" required type="url" placeholder="https://example.com/feed.xml"/><button className="btn btn-dark">Add source</button></div>{e&&<p className="error-msg">{e}</p>}</form>{initial.length>0&&<div className="card" style={{marginTop:20}}>{initial.map(s=><p key={s.id} style={{padding:"8px 0",borderBottom:"1px solid var(--line)"}}><strong>{s.name}</strong> — {s.url}</p>)}</div>}</>}
