import{queryOne}from"@/lib/db";import{DEFAULT_LOCAL_BUSINESS,isLocalBusinessContent}from"@/lib/local-business-content";import LocalBusinessEditor from"./LocalBusinessEditor";export const dynamic="force-dynamic";export default async function Page(){let p:any=null;try{p=await queryOne("SELECT status,content,seo FROM pages WHERE slug='local-business'")}catch{}return <><h1 style={{fontSize:"1.6rem",marginBottom:6}}>Local Business</h1><LocalBusinessEditor content={isLocalBusinessContent(p?.content)?p.content:DEFAULT_LOCAL_BUSINESS} status={p?.status??"draft"} seo={p?.seo??{}}/></>}
