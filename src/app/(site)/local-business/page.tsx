import{redirect}from"next/navigation";import{getPageBySlug}from"@/lib/pages";import BusinessPageView from"@/components/site/BusinessPageView";export const dynamic="force-dynamic";export default async function Page(){const p=await getPageBySlug("local-business");if(!p)redirect("/local-business/index.html");return <BusinessPageView title={p.title} slug="local-business" content={p.content as never}/>}
