import{redirect}from"next/navigation";import{getPageBySlug}from"@/lib/pages";import BusinessPageView from"@/components/site/BusinessPageView";export const dynamic="force-dynamic";export default async function Page(){const p=await getPageBySlug("global-businesses");if(!p)redirect("/global-businesses/index.html");return <BusinessPageView title={p.title} slug="global-businesses" content={p.content as never}/>}
